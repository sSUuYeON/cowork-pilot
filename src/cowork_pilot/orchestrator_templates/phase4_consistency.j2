{# Phase 4-1: 정합성 검사 — §5.4 #}
프로젝트 경로: {{ project_dir }}
데이터/API 섹션 키워드: {{ section_keywords | join(', ') }}

읽어야 할 파일:
- {{ project_dir }}/docs/generated/references/quality-criteria.md (§1 정합성 기준)
- {{ project_dir }}/docs/design-docs/data-model.md
- 모든 product-spec의 데이터/API 관련 섹션 (아래 키워드 기준)
- {{ project_dir }}/docs/ARCHITECTURE.md (디렉토리 구조 섹션)
- {{ project_dir }}/docs/AGENTS.md (Directory Map 섹션)
- {{ project_dir }}/docs/design-docs/index.md
- {{ project_dir }}/docs/product-specs/index.md

정합성 검사를 수행하라.

1. 데이터 정합성: 각 product-spec에서 제목에 다음 키워드가 포함된 섹션을 찾아,
   data-model.md의 엔티티 필드와 일치하는지 교차 검증
   키워드: {{ section_keywords | join(', ') }}
2. 구조 정합성: ARCHITECTURE.md 디렉토리 구조 ↔ AGENTS.md Directory Map 일치 확인
3. 참조 정합성: index.md 목록 ↔ 실제 파일 존재 여부
4. design-docs 간 모순 검사
5. SOURCE 태그 정합성: domain-extracts에서 같은 SOURCE 태그를 가진 내용 간 모순 확인
6. 불일치 발견 시 즉시 수정
7. 결과를 docs/generated/phase4-consistency.md에 저장
8. 파일 마지막 줄에 <!-- ORCHESTRATOR:DONE --> 마커를 기록하라

출력 파일:
- {{ project_dir }}/docs/generated/phase4-consistency.md

품질 규칙:
- docs/ 전체를 통째로 읽지 말고, 교차 검증이 필요한 섹션만 발췌해서 비교하라
- 섹션 번호가 아니라 섹션 제목 키워드로 매칭하라

완료 시 출력 파일의 마지막 줄에 반드시 <!-- ORCHESTRATOR:DONE --> 마커를 기록하라.
