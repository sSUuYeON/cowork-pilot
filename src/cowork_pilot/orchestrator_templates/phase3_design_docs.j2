{# Phase 3-A: design-docs 생성 — §5.3 #}
프로젝트 경로: {{ project_dir }}
대상: design-docs — core-beliefs, data-model, auth, deployment

읽어야 할 파일:
- {{ project_dir }}/docs/generated/references/output-formats.md
- {{ project_dir }}/docs/generated/analysis-report.md
- {{ project_dir }}/docs/generated/domain-extracts/shared.md
{% for feat in features %}
- {{ project_dir }}/docs/generated/gap-reports/{{ feat.domain }}--{{ feat.feature }}.md
- {{ project_dir }}/docs/generated/domain-extracts/{{ feat.domain }}/{{ feat.feature }}.md
{% endfor %}
{% if project_conventions %}
- {{ project_conventions }}
{% endif %}

다음을 수행하라:

각 design-doc에 대해 이 루프를 반복하라:
  ① RE-READ: docs/generated/references/output-formats.md의 design-doc 섹션을 다시 읽는다
  ② RE-READ: 해당 문서에 필요한 gap-report와 domain-extract를 다시 읽는다
  ③ GENERATE: design-doc 1개를 생성한다
     - 입력 원문의 실제 내용을 변환한다. TBD/TODO 사용 금지
     - GUIDE 주석 삽입 금지
     - gap-report의 보강 내용을 반드시 활용한다
  ④ VALIDATE: 방금 쓴 파일을 다시 읽어 검증한다
     - 양식 일치, 금지 표현 부재, 측정 가능 수치 존재
  ⑤ SAVE: 검증 통과 후 저장
  ⑥ DONE: 모든 파일의 마지막 줄에 <!-- ORCHESTRATOR:DONE --> 마커 기록

출력 파일:
- {{ project_dir }}/docs/design-docs/core-beliefs.md
- {{ project_dir }}/docs/design-docs/data-model.md
- {{ project_dir }}/docs/design-docs/auth.md
- {{ project_dir }}/docs/design-docs/deployment.md
- {{ project_dir }}/docs/design-docs/index.md

파일명 규칙 (매우 중요):
- 출력 파일명에서 도메인과 기능 사이의 구분자는 반드시 하이픈 2개(--)를 사용한다.
- 예: ai-agent--nl-parser.md (O), ai-agent-nl-parser.md (X)
- 하이픈 2개(--)를 하이픈 1개(-)로 변경하지 마라. 파서가 깨진다.

품질 규칙:
- "이미 읽었으니 기억한다"는 생각을 하지 말라. 매 문서마다 반드시 RE-READ하라.
- TBD/TODO 사용 금지. GUIDE 주석 삽입 금지.
- 금지 표현: "적절한", "필요시", "충분한", "등등", "추후 작성"

완료 시 모든 출력 파일의 마지막 줄에 반드시 <!-- ORCHESTRATOR:DONE --> 마커를 기록하라.
